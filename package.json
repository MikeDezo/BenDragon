{
  "name": "bendragon-sheets",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types",
    "check": "vite build && wrangler deploy --dry-run",
    "pages:dev": "wrangler pages dev dist --compatibility-flag=nodejs_compat",
    "pages:deploy": "npm run build && wrangler pages deploy dist",
    "start": "node server.js",
    "db:migrate": "node scripts/migrate.js",
    "test": "node test/cloud-storage-test.js",
    "test:db": "node test/postgres-db-test.js"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vite": "^8.2.2",
    "wrangler": "^4.137.0"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "@owlbear-rodeo/sdk": "^3.1.0",
    "cors": "^2.8.6",
    "dotenv": "^18.0.3",
    "express": "^5.2.1",
    "hono": "^4.11.1",
    "pg": "^8.23.0"
  }
}
